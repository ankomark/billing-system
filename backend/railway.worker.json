{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "exec celery -A backend worker -l info --concurrency=2",
    "restartPolicyType": "ON_FAILURE",
    "numReplicas": 1
  }
}
