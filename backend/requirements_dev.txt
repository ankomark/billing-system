# Local development dependencies.
#
# This file used to duplicate the full runtime list, which is how it drifted
# out of sync with requirements.txt. It now inherits instead.
#
#     pip install -r requirements_dev.txt
#
# Local dev can run entirely on SQLite + eager Celery via:
#     python manage.py runserver --settings=backend.settings_local

-r requirements.txt
