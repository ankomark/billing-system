# One image, built once, shared by every service that runs this codebase.
#
# Without the explicit `image:` below, compose gives each service with `build: .`
# its OWN image — backend-web, backend-worker, backend-migrate and so on — built
# independently. `docker compose build web` then rebuilt only the web image and
# left the others on whatever they were, so the Celery worker and beat ran stale
# code, and the one-shot migrate ran an old copy that did not contain the
# migration being deployed. The Dockerfile header already claimed the image was
# shared by all four; naming it here is what makes that true.
x-app: &app
  build: .
  image: wifi-billing-app:latest
  env_file:
    - .env
  volumes:
    # The captive-portal pages, which live at the repository root and so are
    # outside this build context and cannot be COPYed in.
    #
    # They are uploaded to a MikroTik by hand and imported by nothing, so
    # without this the tests that check them find no files and pass vacuously —
    # which is worse than not having them. A broken tag or a mistyped MikroTik
    # variable in there is a blank page in front of a paying customer, and this
    # suite is the only thing that would ever notice.
    - ../mikrotik-hotspot:/mikrotik-hotspot:ro
    # Where the Routers page drops tunnel peer requests for the host to apply.
    #
    # This is the whole of the container's involvement in WireGuard. Adding a
    # peer needs root and the host's network namespace; granting either to a
    # service that answers the internet would mean an RCE in Django came with
    # control of the server's networking, on the box holding the database.
    # A systemd path unit out there watches this directory and does the
    # privileged part — see docker/wg-peer-watcher.sh.
    #
    # Read-write, because the container writes requests. It reads only the
    # .result files the watcher writes back, and nothing outside this path.
    - ${WG_SPOOL_DIR:-/var/spool/wg-requests}:/var/spool/wg-requests

services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: wifi_billing
      # Bootstrap superuser, for administration only — never for the app.
      # Postgres refuses to demote whichever role initdb creates, so this one
      # cannot be made NOBYPASSRLS and therefore cannot be the role Django uses.
      POSTGRES_USER: wifi_admin
      POSTGRES_PASSWORD: ${POSTGRES_ADMIN_PASSWORD:?set POSTGRES_ADMIN_PASSWORD in backend/.env}
      # The unprivileged role 10-app-role.sh creates for Django. These read the
      # same two values the app itself reads from .env, so each credential has
      # one source of truth rather than two that can drift apart.
      APP_DB_USER: ${POSTGRES_USER:?set POSTGRES_USER in backend/.env}
      APP_DB_PASSWORD: ${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD in backend/.env}
    ports:
      # Loopback only. Published as "5432:5432" this listens on every
      # interface, and Docker writes its own iptables rules ahead of ufw — so
      # on a public VPS the database would be reachable from the internet with
      # a firewall that looked correctly configured. Flower already knew this;
      # the two datastores did not.
      #
      # A GUI on the host still connects to localhost:5432. From anywhere else,
      # tunnel: ssh -L 5432:localhost:5432 you@server
      - "127.0.0.1:5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      # Creates the app role without superuser. Without it the app connects as
      # a superuser and every RLS policy is bypassed. See the script.
      - ./docker/postgres-init:/docker-entrypoint-initdb.d:ro
    restart: unless-stopped
    # Without this there is nothing for `condition: service_healthy` to wait on,
    # and dependents race the ~2s postgres takes to accept TCP connections.
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U wifi_admin -d wifi_billing"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s

  redis:
    image: redis:7
    ports:
      # Loopback only — see postgres. Redis is worse if exposed: no password is
      # set, because nothing outside this network was ever meant to reach it.
      - "127.0.0.1:6379:6379"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  # One-shot: runs to completion, then everything else starts. Nothing else in
  # this stack ran migrations, so the whole thing used to come up against an
  # empty database — /health/ passes on one, because it only proves the server
  # answers, not that any table exists.
  #
  # It lives here rather than in web's command because worker and beat need the
  # schema too, and because web runs multiple gunicorn workers — concurrent
  # `migrate` calls race each other on the same database.
  migrate:
    <<: *app
    command: python manage.py migrate --noinput
    depends_on:
      postgres:
        condition: service_healthy
    restart: "no"
    # Inherited from the image, which points at gunicorn on :8000. Nothing
    # listens there in this container. See the note on worker.
    healthcheck:
      disable: true

  web:
    <<: *app
    # gunicorn, not `manage.py runserver` — runserver is single-threaded, does
    # not serve static under DEBUG=False, and is explicitly not for production.
    # collectstatic runs first so WhiteNoise has something to serve (/admin/).
    #
    # Exec-form list, deliberately. This was a `>` folded block, and a folded
    # block does NOT fold lines indented deeper than its first line — it keeps
    # their newlines. Every flag below therefore became its own shell line, and
    # since `exec` replaces the shell, gunicorn started with no arguments at all
    # and none of the later lines ever ran. It fell back to its defaults:
    # 127.0.0.1:8000 and one worker.
    #
    # Binding loopback inside a container means nothing outside it can connect,
    # so the published port answered nothing. The healthcheck passed the whole
    # time — it curls localhost from inside the container, the one place
    # 127.0.0.1 does work — so the container reported healthy while serving no
    # traffic. A list is immune to this; keep it a list.
    command:
      - sh
      - -c
      - "python manage.py collectstatic --noinput && exec gunicorn backend.wsgi:application --bind 0.0.0.0:8000 --workers ${GUNICORN_WORKERS:-4} --timeout 60 --access-logfile - --error-logfile -"
    ports:
      # Loopback only — the same reason postgres and redis are, and this was the
      # one service that missed it. Published as "8000:8000" this listens on
      # every interface, and Docker writes its own iptables rules ahead of ufw,
      # so a VPS with a correct-looking firewall still answers the world on
      # 8000. Verified on a real deploy: the port was reachable from another
      # continent while `ufw status` listed only 22, 80 and 443.
      #
      # Nothing was fully open — ALLOWED_HOSTS rejects a request to the bare IP
      # — but a caller sending the right Host header reached Django in plain
      # HTTP, past the TLS the reverse proxy exists to terminate.
      #
      # The proxy connects over loopback, so it is unaffected. Anything else
      # that needs this port should tunnel: ssh -L 8000:localhost:8000 you@server
      - "127.0.0.1:8000:8000"
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully

  worker:
    <<: *app
    command: celery -A backend worker -l info
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    # The image's HEALTHCHECK curls gunicorn on :8000. A celery worker serves no
    # HTTP, so it inherited a check that can never pass and sat permanently
    # unhealthy — which would keep it out of any orchestrator's rotation and
    # makes `--wait` never return. Celery has no cheap liveness probe worth the
    # broker round-trip here, so disable rather than substitute.
    healthcheck:
      disable: true

  beat:
    <<: *app
    command: celery -A backend beat -l info
    # beat is the one service whose death is silent: no scheduler means no
    # expiry sweeps or reminders, and nothing else errors. It previously had
    # neither an ordering guarantee nor a restart policy, so losing the startup
    # race with postgres killed it for the lifetime of the stack.
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    healthcheck:
      disable: true

  flower:
    <<: *app
    # Credentials come from FLOWER_BASIC_AUTH in .env — never hardcode them here,
    # this file is committed. The `:?` guard makes compose abort with the message
    # below if the variable is unset or empty; without it an empty --basic_auth
    # would start Flower with NO authentication at all, silently exposing every
    # task payload (phone numbers, M-Pesa references) on port 5555.
    command: >
      celery -A backend flower
      --address=0.0.0.0
      --port=5555
      --basic_auth=${FLOWER_BASIC_AUTH:?set FLOWER_BASIC_AUTH=user:password in backend/.env}
    # Bound to loopback: Flower has no TLS and only weak basic auth, so it should
    # not be reachable from outside the host. Reach it via an SSH tunnel:
    #   ssh -L 5555:localhost:5555 you@server
    # Change to "5555:5555" only if you front it with a TLS-terminating proxy.
    ports:
      - "127.0.0.1:5555:5555"
    restart: unless-stopped
    depends_on:
      redis:
        condition: service_healthy
      worker:
        condition: service_started
    # Flower listens on 5555, not 8000, so it inherited the same unpassable
    # check as worker. It does expose /healthcheck, and that endpoint is served
    # without basic auth, so no credentials need to appear in this file.
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:5555/healthcheck"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

volumes:
  pgdata:
